{
  "name": "@carmediahub/plugins",
  "version": "0.1.0",
  "private": true,
  "description": "Official and community plugin collection for CarMediaHub.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.19.0",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json && node scripts/build-packages.mjs",
    "test": "tsx --test src/**/*.test.ts plugins/**/*.test.ts && node --test scripts/catalog-packages.test.mjs scripts/verify-packages.test.mjs",
    "verify:packages": "node scripts/verify-packages.mjs",
    "verify": "pnpm check && pnpm test && pnpm build && pnpm verify:packages",
    "sbom": "node scripts/generate-sbom.mjs"
  },
  "dependencies": {
    "@carmediahub/sdk": "file:../carmediahub-sdk"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "ajv": "8.17.1",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22"
  }
}
